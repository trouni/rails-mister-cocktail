<%= cl_image_tag @cocktail.photo, width: "300px", height: "200px", class: "m-3" %>

<%= simple_form_for @cocktail do |f| %>
  <%= f.input :photo, label: false %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit "Upload", class: "btn btn-primary" %>
<% end %>
